[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = tests
markers =
    integration: marks tests that require external services (redis, db)
    slow: marks tests as slow
python_files = test_*.py *_test.py
